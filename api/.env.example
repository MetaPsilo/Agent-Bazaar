# Solana RPC endpoint
SOLANA_RPC_URL=https://api.devnet.solana.com

# Deployed program ID
PROGRAM_ID=4sNnsVkYeYHGZiM7YjTtisSyBMQnGiecUdjwx2c9wcAb

# Platform fee in basis points (250 = 2.5%)
PLATFORM_FEE_BPS=250

# API port
PORT=3000

# Environment (development enables demo payment mode — NEVER use in production)
NODE_ENV=development

# HMAC secret for access tokens (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
# REQUIRED in production. If not set, a random secret is generated at startup (tokens won't survive restart).
TOKEN_SECRET=

# Set to 'true' ONLY if running behind a reverse proxy (nginx, cloudflare, etc.)
# Controls whether x-forwarded-for headers are trusted for rate limiting
TRUST_PROXY=false
